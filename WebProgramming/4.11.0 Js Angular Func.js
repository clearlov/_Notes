angular
        /**
         * same as <dom ng-app="modules">
         */
        .bootstrap(DOMElement dom, [Array<String|Function|Array> modules], [conf]
        .lowercase(str)
        .uppercase(str)
        .copy(src, [dst])
boolean .equals(Object o1, Object o2)
string  .fromJson(json) // deserialize a json
boolean .isArray(val)
boolean .isDate(val)
boolean .isDefined(val)
boolean .isUndefined(val)
boolean .isElement(val) // a DOM element
boolean .isFunction(val)
boolean .isNumber(val)
boolean .isObject(val)
boolean .isString(val)
jQuery  .element
     
function con(callback, val){
  var r = calculateResult();
  (callback || angular.noop)(r);
  (callback || angular.identity)(val);
}

/**
 * angular.injector(Array<string|Function> modules, bool strictDI = false) 
 * is used to retrieve object instances as defined by provider, instantiate 
 *  types, invoke methods, and load modules.
 */
var injector = angular.injector([]);





  .forEach(array|json, function(v,k){}, [Object context])
  .extend()
  
  
  
  
  
